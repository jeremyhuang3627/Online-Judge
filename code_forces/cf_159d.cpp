#include <iostream>
#include <cstdio>
#include <algorithm>
#include <vector>

#define F(i, a, b) for( int i = (a); i < (b); i++ )
#define min(a, b)  (a > b ? b : a)

using namespace std;

string s;
int l[2002]; // number of palindromes on the left at the separation;
int r[2002]; // number of palindromes to the right;

int t [2002][2002];

int main(){
	cin >> s;
	

	return 0;
}