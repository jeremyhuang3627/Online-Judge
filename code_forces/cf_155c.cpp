#include <iostream>
#include <cstdio>
#include <algorithm>
#include <vector>
#include <string>

#define F(i, a, b) for( int i = (a); i < (b); i++ )
#define min(a, b)  (a > b ? b : a)
#define M 100005;

using namespace std;

int n,numDel;
int del[M];
string s;
string a [13];

int minDel(int i,int j){
	if()
}

int main(){

	cin >> s >> n;
	F(i,0,n){
		cin >> a[i];
	}

	cout << minDel(0,1) << endl;
	return 0;
}